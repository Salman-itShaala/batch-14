const doSomething = () => {
  console.log("Doing something");
};

doSomething();

const addNumbers = (num1, num2) => {
  return num1 + num2;
};

const addNumbers2 = (num1, num2) => num1 + num2;

const convertToUpperCase = (str) => str.toUpperCase();

console.log(convertToUpperCase("salman"));
